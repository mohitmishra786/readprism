# Web framework
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32

# Database
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
psycopg2-binary==2.9.12
alembic==1.18.5
pgvector==0.5.0

# Auth
bcrypt==4.3.0
pyjwt==2.13.0

# Settings
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0

# Cache / Queue
redis==5.3.1
celery==5.6.3

# LLM — Primary
groq==0.37.1

# LLM — Fallback (only loaded when OPENAI_FALLBACK_ENABLED=true)
openai==1.109.1

# Embeddings
sentence-transformers==3.4.1
numpy==1.26.4

# Ingestion
feedparser==6.0.12
httpx==0.28.1
playwright==1.61.0
beautifulsoup4==4.15.0
lxml==6.1.1
listparser==0.20
trafilatura==1.12.2

# Email
jinja2==3.1.6

# Testing
# pytest 9 is incompatible with pytest-asyncio (which requires pytest<9,>=8.2).
# Pin to the latest 8.x until pytest-asyncio adds pytest 9 support.
pytest==8.4.2
pytest-asyncio==0.26.0
respx==0.23.1
httpx==0.28.1

# Search — Meilisearch is integrated via raw httpx in app/utils/search.py;
# the official SDK pin (3.3.3) referenced a nonexistent release and was never imported.

# Utilities
python-dotenv==1.2.2
